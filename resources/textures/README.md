# Placeholder textures

В текущем MVP здесь уже лежит:

- `enemy.bmp` - спрайт второго игрока с chroma-key фоном `#00ff00`.
- `weapon.bmp` - first-person спрайт оружия игрока с chroma-key фоном `#00ff00`.

На следующих этапах сюда стоит добавить остальные простые ресурсы:

- `wall_stone.bmp`
- `floor_stone.bmp`
- `sky.bmp`

Для MVP стены, пол и небо всё ещё используют процедурные цвета. Это осознанное упрощение: сначала доводим архитектуру, карту, raycasting, столкновения и боевую логику, а потом добавляем полноценные текстуры.
